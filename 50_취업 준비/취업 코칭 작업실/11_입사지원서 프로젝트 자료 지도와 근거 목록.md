---
type: project-doc
status: active
created: "2026-09-07"
project: 취업 코칭
project_moc: "[[50_취업 준비/취업 코칭 작업실/00_취업 코칭 MOC]]"
---

# 입사지원서 프로젝트 자료 지도와 근거 목록

## 목적과 이번 확인 범위

[[50_취업 준비/입사지원서_장운호_작업본|지원서 작업본]]의 `# 프로젝트`에 필요한 자료를 선별하고, 이후 자기소개서와 프로젝트를 다시 쓸 때 원본 전체를 재독하지 않도록 근거 위치를 남긴다. 이번 단계에서는 지원서 원문을 수정하지 않았다.

- 읽기 순서: 기존 준비 문서 → 항목별 근거 목록 → 관련 코드 일부 → 필요한 실행 증적.
- `D:\kisa-webapp-checker`는 사용자 지시로 이번 작업에서 제외한다.
- 전체 RAW·로그·PDF·보고서를 전수 열람하지 않았다. AWS·Docker·GitHub 실행이나 환경 변경도 하지 않았다.
- 구현 확인, 과거 실행 기록, 이번에 직접 확인한 증적을 구분한다. 기록이 미확인이라는 이유로 기능이 미구현이라고 단정하지 않는다.
- 개인 기여의 출발점은 [[50_취업 준비/취업 코칭 작업실/10_입사지원서 준비 현황과 3차 프로젝트 정리|기존 준비 문서]]의 사용자 확인 기록이다. 장운호가 AI를 보조 도구로 사용해 구축 전반을 수행했다고 적혀 있다. 이를 모든 원본 코드의 단독 저작으로 확대하지 않는다.

## 지원서 항목별 읽을 자료

| 채울 항목 | 우선 자료 | 여기서 확인할 내용 |
|---|---|---|
| 기본정보·서비스 목표·사용기술 | 기존 준비 문서, Terraform `data.tf`, 최종 관제 요약 | AWS·EKS 관제 중심 목적, RDS MariaDB, 실제 사용 범위. 총인원은 아직 빈칸 |
| 주요역할 설계 | `foundation/`, `daily-down.ps1`, Wazuh Push 구성, 탐지 규칙, 대응 계약 | 운영 수명주기·수집 경로·단계별 탐지·대응 범위를 어떻게 설계했는가 |
| 구현기능 서비스 | Wazuh Rule, Custom Integration, Shuffle App, DVWA Workflow·Helm Template | 로그 전달부터 경보 검증·Pod 격리·방어 수준 전환·복구까지 실제 코드 |
| 프로젝트 참여소감 | 운영 실패 사례, 정상 대조 실험 회고, 7장 결론의 판단 변화 | 문제를 발견한 과정, 조치의 이유, 검증에서 배운 점. 현재 시점의 소감은 작성 시 본인 표현으로 정리 |
| 아키텍처 설계 1 | Terraform Root·Foundation·Daily 운영 자료 | 재현 가능한 AWS·EKS 실습환경과 보존/삭제 경계 |
| 아키텍처 설계 2 | Wazuh Push 설계, Rule, 로그 경로 그림 | 정기 수집과 DVWA 저지연 전달, 서로 다른 공격 단계의 탐지 |
| 아키텍처 설계 3 | Shuffle·DVWA Workflow, 자동 대응 폐쇄루프 그림 | 경보 검증 → 고정 대응 → GitOps 배포 → 결과 확인·Reset |

세 아키텍처 주제는 현재 빈 양식에 맞춘 후보이며, 최종 그림 선택은 아직 하지 않았다. ECS Fargate·지하철 스토리·Supabase 등 기존 예시는 프로젝트 근거로 사용하지 않는다.

## 자료 위치

| 위치 | 역할 | 이번 확인 |
|---|---|---|
| `D:\terraform\aws_terraform_build_code` | AWS·Terraform·운영 자동화·Wazuh/Shuffle 연계 코드 | 파일 목록, Git 요약, 선별 코드 |
| `D:\DVWA` | 애플리케이션·GitHub Actions·Helm·Argo CD 대응 구성 | 선별 구현 조사 |
| `D:\Wazuh\wazuh-docker\single-node` | 로컬 Wazuh 배포·설정 경로 | `capital_one_rules.xml` 핵심 조건 확인. Runtime·설정 전수 확인 없음 |
| `C:\Users\Unoh\Documents\aws-topology-evidence` | 실험별 Bundle·실행 증적의 주 위치 | 디렉터리 목록과 지정 Manifest 2개 |
| `D:\terraform\aws-topology-evidence` | 별도 SOC 증적 위치 | 최상위에 `shuffle-observe-only` 폴더 확인 |
| `20_팀 프로젝트/3차 프로젝트/` | 보고서·시연 근거·검증 기록·그림 | 파일명·목차 및 지정 문단 |

기억에 있던 D드라이브 증적 경로만 조사하면 정상 대조 실험과 Daily Down Bundle을 놓친다. 다음 조사에서는 C드라이브의 실험 식별자로 바로 들어간다.

## 핵심 경험과 근거

### 1. 보존 자원과 매일 지울 자원의 수명주기 분리

- 문제: 반복 실습 비용을 줄이면서 이미지·배포 연결·로그는 보존해야 했다.
- 구현 근거: `D:\terraform\aws_terraform_build_code\foundation\main.tf:87`의 ECR, `:126`의 GitHub OIDC, `:162`의 IAM Role. `일일_수동_운영_매뉴얼.md:123`부터 Daily와 Foundation 상태를 별도로 확인하는 절차가 있다.
- 과거 실행 기록: [[20_팀 프로젝트/3차 프로젝트/관측성_As-built_및_Runtime_검증|관측성 검증 기록]] `:187`에 Daily Runtime 250개 제거, Daily State·태그 Runtime 0, Foundation 27개 보존이 기록돼 있다.
- 직접 확인한 증적: `C:\Users\Unoh\Documents\aws-topology-evidence\observability-20260802T131345Z-final-post-destroy\manifest.json`에서 `Phase=post-destroy`, 수집 결과 8개 `Succeeded`, `MissingContext=[]`를 확인했다. 동반 SHA-256과 Manifest 파일의 해시가 일치했다.
- 한계: Manifest는 삭제 후 로그 수집 증적이다. 이것만으로 Runtime 0을 독립 검증한 것은 아니다. 이번에는 전체 Bundle 해시나 AWS 현황을 재검증하지 않았다.
- 배치 후보: 주요역할·아키텍처 1, 자기소개서 습득 기술.

### 2. Terraform State 밖의 Karpenter 자원 정리

- 문제: 동적으로 만들어진 NodeClaim·EC2가 삭제를 방해하는 사례를 지원서 초안과 운영 문서에서 확인했다.
- 구현 확인: `daily-down.ps1:495`에서 Terraform 삭제 실행(`:508`) 전에 `Invoke-KarpenterPreDestroyCleanup`을 호출한다.
- 안전 범위: `automation/Karpenter.Cleanup.psm1:64` 이후 Cluster·NodeClaim·NodePool·Project·ManagedBy 태그를 판정하고, `:230`에서 NodePool·NodeClaim 삭제, `:325`에서 제한 시간과 종료 대기를 처리한다.
- 한계: 정리 모듈의 존재·호출 순서는 확인했다. 보완 코드 적용 후 전체 Up→Down 재실행 성공은 이번 조사에서 입증하지 않았다.
- 배치 후보: 구현기능·참여소감, 자기소개서 문제 해결 사례.

### 3. 공격 성공뿐 아니라 정상 대조군으로 탐지 조건 확인

- 근거 문서: `D:\terraform\aws_terraform_build_code\report\CAPITAL-ONE-GATE3-VALIDATION-LESSONS.md:28`부터 2026-08-12 결과가 정리돼 있다.
- 기록된 결과: 같은 가짜 S3 객체를 정상 운영자가 조회했을 때 CloudTrail 성공 이벤트 1건이 남고 Capital One Alarm은 OK를 유지했다. 노드 역할 공격의 탐지와 대비한 실험이다.
- 직접 확인한 증적: `C:\Users\Unoh\Documents\aws-topology-evidence\capital-one-negative-20260812T034935Z\manifest.json`에서 `capital-one-negative-control=Succeeded`, `MissingContext=[]`, Manifest 해시 일치를 확인했다.
- 한계: 이번에는 원본 이벤트와 Alarm 전후 상태까지 다시 대조하지 않았다. 이 실험은 당시 Capital One Alarm에 대한 결과이며, 최종 Wazuh Rule `100111`의 정상 대조 검증으로 바꿔 쓰면 안 된다. 모든 정상 사용자에 대한 오탐 부재를 증명하지도 않는다.
- 배치 후보: 자기소개서 강점, 프로젝트 참여소감.

### 4. DVWA 이벤트 전달 지연을 줄이고 측정 범위를 구분

- 기록 근거: `D:\terraform\aws_terraform_build_code\observability\wazuh\WAZUH-PUSH-TRANSPORT-DESIGN.md:257` 및 [[20_팀 프로젝트/3차 프로젝트/보고서/7장 결론 고정 구조 및 내용 보존표|7장 판단 과정]] `:90`.
- 기록된 결과: 무해 이벤트 N=3에서 Event→Rule `100102` Alert가 6.439 / 3.427 / 3.761초, 누락 0건.
- 원본 재확인 시작점: `C:\Users\Unoh\Documents\aws-topology-evidence\wazuh-push-validation`의 해당 세 `*-source.json`; 대응 Live Ledger와 Wazuh Alert를 함께 찾아야 한다. 이번에는 파일 존재까지 확인했다.
- 한계: 공격 탐지 Rule `100110`이나 Shuffle·GitHub·Argo까지 포함한 전체 대응 시간으로 제시하지 않는다. 정기 수집의 10분/5분 표기는 시점별 자료가 다르므로 최종 설명에서는 적용 시점을 명시한다.
- 배치 후보: 아키텍처 2, 참여소감, 자기소개서 판단 과정.

### 5. 공격 단계에 따라 Wazuh 탐지 조건 분리

- 구현 근거: `D:\Wazuh\wazuh-docker\single-node\config\wazuh_cluster\rules\capital_one_rules.xml:45`의 Rule `100110`은 DVWA Push, 명령 실행 성공, IMDS, low, `imds_credential_fetch`, `output_returned` 조건을 요구한다. 자격증명 엔드포인트 대상 명령의 출력 반환을 식별하는 조건이다.
- 같은 파일 `:64`의 Rule `100111`은 CloudTrail의 S3 GetObject Data Event, 지정 계정·리전·노드 역할·객체 Key, Bucket 이름 패턴, HTTP 200, 양수 반환 바이트와 Event ID 형식을 요구한다.
- 확인 수준: 두 Rule의 소스 조건을 직접 확인했다. 실제 Alert와 동일 TAKE를 대조하지 않았다. 두 경보가 코드만으로 하나의 공격 인과관계에 자동 연결되는 것은 아니다.
- 배치 후보: 주요역할의 탐지 설계, 구현기능, 아키텍처 2.

### 6. 경보 검증과 사전 정의된 대응만 허용

- 구현 근거: `D:\terraform\aws_terraform_build_code\observability\wazuh\integrations\custom-shuffle-soc:227` 이후 100110 전달 계약, `:344` 이후 100111 계정·리전·역할·객체 등 조건을 재검증한다.
- Shuffle 근거: `observability/shuffle/apps/aws-topology-soc-rule100110-auto-containment/1.0.1/src/autocontainment.py:146` 이후 Payload 검증, `:200` 이후 100110 신선도 제한, `:239`에 100111 Event 1,200초 제한, `:268` 이후 Rule별 고정 Workflow 분기가 있다.
- 재검토한 주의점: 통합 코드의 `EXPECTED_BUCKET_NAME` 상수만 보면 단일 Bucket 검증으로 오독할 수 있다. 현재 100111 경로 `:361`은 `PRIMARY_BUCKET_PATTERN`을 검사한다. 상수 존재와 실제 호출 경로를 구분한다.
- 확인 수준: 구현 계약과 핵심 분기를 확인했다. Dispatch 성공이 실제 격리·배포 완료를 뜻하지 않는다.
- 배치 후보: 주요역할의 대응 범위 설계, 구현기능, 아키텍처 3.

### 7. 침해 Pod 격리·보존과 GitOps 방어 수준 전환

- 격리 근거: `D:\DVWA\.github\workflows\soc-contain-dvwa.yml:83` 이후 선언 값 변경과 증적 생성. `deploy/dvwa/templates/quarantine-job.yaml:100` 이후 Pod UID와 소유 관계를 확인하고 `:124`에서 격리 Label 변경·OwnerReference 제거를 수행한다. `networkpolicy-quarantined-pod.yaml:8` 이후 해당 Label을 선택해 Ingress·Egress를 모두 차단하도록 선언한다.
- 완화 근거: `D:\DVWA\.github\workflows\soc-harden-dvwa.yml:66`은 전용 전환 스크립트를 호출하고, `:73`에서 변경 경로를 `deploy/dvwa/values.yaml` 하나로 제한한다. `:99`에 Commit, `:119`에 `target_level=impossible` 증적 생성이 있다.
- 확인 수준: Workflow·Helm의 핵심 구현을 확인했다. 실제 네트워크 차단과 새 Session의 impossible 상태는 동일 실행의 Runtime 증거로 별도 확인해야 한다.
- 표현 경계: 사전 구현된 DVWA 방어 프로필 전환이다. 자동 취약점 수정 코드 생성, 탈취 자격증명 폐기 또는 공격 완료 전 차단을 구현했다고 쓰지 않는다.
- 배치 후보: 구현기능, 아키텍처 3, 참여소감의 대응 검증.

## 그림 후보와 추가 확인 경계

다음 파일은 존재와 draw.io 페이지 이름만 확인했다. 시각 품질·최종 구현과의 일치 여부는 아직 검토하지 않았다.

| 후보 | 확인한 페이지 | 사용할 때 필요한 확인 |
|---|---|---|
| `20_팀 프로젝트/3차 프로젝트/architecture/BANK_AWS_Security_Lab_Whole_Infrastructure.drawio` | `01_전체 인프라` | 최종 배포 범위, Foundation/Daily 구분, 과거 DR·선택 기능 포함 여부 |
| `20_팀 프로젝트/3차 프로젝트/architecture/로그_시스템_다이어그램.drawio` | `02_로그와 대응` | 최신 Push 경로와 Rule 조건 일치 여부 |
| `20_팀 프로젝트/3차 프로젝트/노션_내보낸거/5.0 Capital One SOC 자동 대응 폐쇄루프.drawio` | 탐지 경로·대응 폐쇄루프 페이지 | 최종 Workflow·격리·완화 범위 일치 여부 |

## 다음에 필요한 것만 읽기

1. 주요역할·구현기능은 이 목록의 코드 근거로 초안을 작성한다. 새로운 기술 전수조사는 하지 않는다.
2. 최종 자동 대응을 완료 성과로 쓸 때만 [[20_팀 프로젝트/3차 프로젝트/시연 장면별 근거 파일|시연 장면별 근거 목록]]의 SC-03·SC-05·재검증 항목을 따라 같은 TAKE의 Alert→Shuffle Execution→GitHub Run→Commit→Argo Revision→Pod 결과를 연결한다.
3. 인원은 기존 준비 문서와 지원서에 모두 비어 있으므로 사용자 확인이 필요하다.
4. 자기소개서 웹 취약점 경험은 이번 3차 프로젝트 근거 목록만으로 입증하지 않는다. 그 문항을 다시 쓸 때 해당 경험 자료를 별도로 좁혀 확인한다.

이번 범위에서 제외한 것: 기존 원고 수정, DOCX 편집, 그림 재작성, MOC 변경, 전체 증적 재감사, 테스트 재실행, 클라우드 재기동.

## 확인 기준점

- 조사 시작 Vault HEAD: `347ef378a361964daca707ef9b85d7f8e3cc9cfa`.
- Terraform HEAD: `cdf3f32`, DVWA HEAD: `86329b3`. 조회 시 두 저장소 작업 트리는 깨끗했다.
- 서브에이전트 1개가 SOC 구현을 선별 조사했고, 주 에이전트가 Rule 조건·전달 계약·Workflow 분기·Pod 격리·완화 경로의 핵심을 재확인했다.
- 새 노트의 frontmatter, wiki-link 대상 존재, 공백 오류를 검사한다. 이 검사는 기술 기능의 재실행 검증을 대신하지 않는다.
