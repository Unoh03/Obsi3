---
type: map
topic: web-security
source: 5-20_웹보안.pdf
source_pages:
  - "1-136"
status: draft
created: 2026-05-20
reviewed:
aliases:
  - 5-20 웹보안 구조 지도
  - 웹보안 PDF 구조 지도
tags:
  - 🏷️과목/웹보안
  - 🏷️주제/PDF-Map
  - 🏷️상태/draft
---

# 5-20 웹보안 PDF 구조 지도

source: [[40_자료/강의 자료/5-20_웹보안.pdf|5-20 웹보안]], p.1-136
raw memo: [[10_학습 노트/시스템보안/서론(이라쓰고 빠르게 휘갈겨 쓴거)|서론 raw 메모]]

## 목적

이 노트는 `5-20_웹보안.pdf`를 바로 요약하기 위한 노트가 아니라, 앞으로 세부 정리 노트를 만들 때 사용할 **page range와 주제 경계 지도**다.

작성 기준:

- 1순위 근거: PDF에서 추출한 page heading과 단원 번호
- 2순위 근거: raw 수업 메모의 질문과 강조
- 3순위 근거: 기존 웹보안 목차와 이미 작성한 노트의 cross-check

기존 노트를 기준으로 PDF를 끼워 맞추지 않는다. 충돌이 있으면 이 구조 지도에 `기존 목차와 충돌 가능`으로 표시하고, 노트 작성 전에 다시 판단한다.

---

## 구조 지도 정책

- 이 지도는 최종 노트 목록이 아니라 PDF의 중간 단위 주제 경계 지도다.
- PDF의 단원 순서는 유지한다.
- 하나의 PDF 단원이 실제 노트로는 너무 크면, 행을 무조건 쪼개지 않고 `노트화 시 분리 후보`를 분리/병합 판단에 표시한다.
- 단, PDF 안에서 별도 heading이 있고 원인·영향·대응이 다른 취약점은 중간 지도에서도 하위 분리로 명시한다.
- 최신성 또는 공식 기준이 중요한 내용은 PDF를 1차 강의 자료로 보되, 공식 문서로 정정/보강한다.
- 공격 예시는 실습·방어 이해 목적의 최소 맥락으로만 남긴다.

---

## PDF 단원 경계 근거

| 시작 page | PDF에서 관찰한 표식                    | 판단                             |
| ------- | ------------------------------- | ------------------------------ |
| p.1     | `Web Application 해킹 & DB 보안`    | 표지                             |
| p.2     | `1. World Wide Web`             | 웹 기본 개요 시작                     |
| p.3     | `1. Web의 구조`                    | 웹 애플리케이션 구조                    |
| p.4     | `1. HTTP`                       | HTTP 단원 시작                     |
| p.24    | `2. HTML`                       | HTML / Web Language 단원 시작      |
| p.35    | `3. Encoding Schema`            | 웹 인코딩 단원 시작                    |
| p.44    | `4. Type of Web Authentication` | 웹 인증 단원 시작                     |
| p.49    | `5. Session Token`              | 세션/쿠키 단원 시작                    |
| p.62    | `6. OWASP`                      | OWASP Top 10 단원 시작             |
| p.73    | `7. Web Spidering`              | 정보수집/검증 우회/Brute Forcing 단원 시작 |
| p.80    | `8. Web Session Hijacking`      | 세션 하이재킹 단원 시작                  |
| p.83    | `8. XSS(Cross Site Scripting)`  | XSS 단원 시작                      |
| p.98    | `9. CSRF`                       | CSRF 단원 시작                     |
| p.107   | `10. SQL`                       | SQL 기초 단원 시작                   |
| p.115   | `11. SQL Injection`             | SQL Injection 단원 시작            |
| p.133   | `12. File Upload 취약점`           | File Upload / Webshell 단원 시작   |
| p.136   | `12. Directory Listing 취약점`     | Directory Listing 단원           |

---

## 중간 단위 파트 분해

| Page range | PDF에서 관찰한 제목/표식 | 추천 노트 제목 | 우선순위 | 분리/병합 판단 | 확신도 | 확인 필요 질문 |
| --- | --- | --- | --- | --- | --- | --- |
| p.1-3 | `World Wide Web`, `Web의 구조` | [[웹 애플리케이션 구조]] | 완료 | p.2의 WWW 개요는 별도 노트보다 구조 노트의 배경으로 흡수하는 편이 적절 | 높음 | WWW 자체를 별도 개념 노트로 뺄 필요가 있는가? 현재는 낮음 |
| p.4-23 | `HTTP`, `HTTP Packet Layout`, `Request/Response`, `Header Fields`, `GET/PUT/Delete Method` | [[HTTP 구조와 메시지]], [[HTTP Method와 Header]] | 완료 | PDF상 한 단원이지만 읽기 부담 때문에 2개 노트로 분리한 현재 구조가 적절 | 높음 | p.21-23 method 설명을 Method/Header 노트가 충분히 흡수했는가? |
| p.24-34 | `HTML`, `Web Language`, `HTML DOM`, `Javascript`, `VBScript`, `ASP`, `ASP.NET`, `PHP`, `JSP` | [[웹 문서와 실행 위치]] | 완료 | DOM과 client/server 실행 위치를 우선 1개 stable concept note로 정리했다. DOM 상세 또는 server-side runtime 상세가 길어지면 `DOM과 Client-side Script`, `Server-side Script와 WAS 실행 흐름`으로 후속 분리를 검토한다. PHP 약어 등 오래되거나 부정확한 설명은 공식 문서로 정정했다. | 높음 | 현재는 분리하지 않는다. 후속 실습/개념 노트가 비대해질 때만 분리 |
| p.35-43 | `Encoding Schema`, `ASCII`, `URL Encoding`, `URL Meta Character`, `Force Full URL Encoding`, `HTML Encoding`, `EUC-KR`, `Unicode`, `Base64 Encoding` | [[HTML 인코딩]] | 완료 | 파일명은 기존 XSS/실습 연결을 보존하기 위해 유지하되, p.35-43 전체를 웹 인코딩 범위로 확장했다. URL/Unicode/Base64는 상세 하위 노트가 필요해질 때만 분리한다. | 높음 | 현재는 분리하지 않는다. |
| p.44-48 | `Type of Web Authentication`, `Basic Authentication`, `Anonymous Authentication`, `Form Based Authentication` | 웹 인증 구조 | 높음 | 인증 방식 비교 노트 1개로 적절. 실제 노트 제목 후보는 `기초 웹 인증 방식 - Basic, Anonymous, Form`. 짧은 제목을 원하면 `Basic/Form 기반 웹 인증`도 가능하지만, Anonymous Authentication을 본문에서 짧게라도 다룬다. | 높음 | Basic은 “안 씀”으로 끝내지 말고 Base64와 평문 노출 위험을 어떻게 설명할지 확인 |
| p.49-61 | `Session Token`, `Web Session`, `Session Management`, `Hidden Field`, `URL Rewriting`, `Cookie`, `Persistent Cookie vs Session Cookie` | 세션과 쿠키 | 높음 | 분량이 많고 중요함. 우선 1개 노트로 만들되, 길어지면 `Session Token`과 `Cookie`를 분리 | 높음 | raw 메모의 “너랑 나도 세션인가?”를 일반 session과 web session 차이로 풀어야 함 |
| p.62-72 | `OWASP`, A01-A10 진단 관점 | [[OWASP Top 10 2025]] | 완료 | PDF p.62-72의 강의식 진단 질문을 보존하고, 공식명과 2025 변경점은 OWASP 공식 문서 기준으로 맞췄다. Top 10은 세부 실습 노트를 대체하지 않고 취약점 분류·복습 라우팅 기준으로 쓴다. | 높음 | 현재는 완료. 후속 실습에서 A03/A06/A08/A09/A10 하위 노트가 필요해질 때 분리 |
| p.73-76 | `Web Spidering`, `Type of Validation`, `Bypassing Client Side Validation` | [[웹 진단 기초 - Spidering과 Client-side Validation 우회]], [[Client-side Validation 우회와 Server-side Validation 실습]] | 완료 | stable concept note는 spidering 한계와 validation 신뢰 경계를 담당한다. 기존 lab note는 Paros `Trap request` / `Trap response` 실습 증거로 유지한다. | 높음 | 현재는 concept/lab 분리로 충분하다. |
| p.77-79 | `ID/PW Brute Forcing`, `Hydra`, 대응책 | 로그인 Brute Force와 계정 보호 | 중간 | 짧은 공격/방어 노트 1개로 적절하다. Hydra 예시는 도구 사용법 중심이 아니라 실패 메시지 제한, 계정 잠금, 로그인 지연, 동시접속 제어 같은 방어책을 설명하기 위한 실습 맥락으로 제한한다. | 높음 | 계정 보호 대책을 어느 정도까지 현대적으로 보강할지 확인 필요 |
| p.80-82 | `Web Session Hijacking`, `Session Token 획득 방법` | Web Session Hijacking | 높음 | PDF 구조 지도에서는 p.80-82 위치를 유지한다. 다만 학습/작성 순서는 `세션과 쿠키 -> XSS -> Web Session Hijacking`을 권장한다. 세션 토큰 획득 경로로 XSS와 Sniffing이 직접 연결되기 때문이다. | 높음 | Sniffing/XSS로 token을 얻는 흐름을 어느 노트에 링크할지 결정 |
| p.83-97 | `XSS`, `Type of XSS`, `Reflective`, `Stored`, 취약 페이지, Filtering 우회, Cookie Stealing, 대응책 | XSS | 매우 높음 | 1개 긴 노트로 시작하되, 필요하면 `XSS 유형`, `XSS 필터링 우회`, `Cookie Stealing`을 하위 노트로 분리 | 높음 | 공격 payload 나열이 아니라 문맥별 출력 인코딩과 방어 중심으로 정리해야 함 |
| p.98-106 | `CSRF`, `XSS와 CSRF의 차이점`, 실습, 대응책 | CSRF | 높음 | XSS와 비교하는 독립 노트 1개로 적절 | 높음 | SameSite, CSRF token 같은 현대 방어를 어디까지 추가할지 공식 문서 확인 필요 |
| p.107-114 | `SQL`, 데이터 조회, SQL 문법 | SQL 기초 | 중간 | SQL Injection을 이해하기 위한 prerequisite 노트로 작성 | 높음 | 이미 SQL을 알면 짧게 만들고, 모르면 SELECT/WHERE 중심으로 보강 |
| p.115-132 | `SQL Injection`, 인증우회, 변환 에러, UNION, DB Schema, 대응책 | SQL Injection | 매우 높음 | 중간 지도에서는 `SQL Injection` 한 범위로 유지한다. 단, 실제 노트화 시 분리 강도는 높음. 최소 분리 후보는 `SQL Injection 개념과 인증 우회`, `Error/UNION 기반 정보 추출과 Schema 파악`, `SQL Injection 방어`다. PDF의 필터링/WAF 중심 대응책은 그대로 두지 말고 Prepared Statement / Parameterized Query를 1차 방어로 보강한다. | 높음 | 실습 payload는 학습/방어 맥락으로 제한하고 prepared statement를 반드시 보강 |
| p.133-136 | `File Upload 취약점`, `웹쉘 Upload`, `Directory Listing 취약점` | File Upload와 Webshell / Directory Listing | 높음 | 중간 지도에서는 Chapter 12 묶음으로 유지할 수 있으나, 실제 노트화 시 `p.133-135 File Upload와 Webshell`, `p.136 Directory Listing 취약점`은 분리한다. File Upload는 업로드된 서버 측 코드 실행 위험이고, Directory Listing은 웹 서버 설정 오류로 인한 파일 목록 노출 문제라 원인·영향·대응이 다르다. | 중간 | Directory Listing은 p.136만으로 단독 노트 초안은 가능하지만, Apache/IIS/Nginx 설정 예시는 추가 자료로 보강할 필요가 있는가? |

---

## 기존 목차와 다른 점

- `HTTP`는 PDF 기준으로 p.4-23 한 단원이지만, 현재 노트는 `HTTP 구조와 메시지`와 `HTTP Method와 Header`로 분리되어 있다. 충돌이라기보다 학습 편의상 좋은 분리다.
- 기존 목차의 `HTML DOM과 클라이언트 스크립트`는 PDF p.24-34 전체를 담기에는 제목이 좁을 수 있다. 이 범위에는 ASP, ASP.NET, PHP, JSP 같은 server-side script도 들어간다.
- 기존 목차의 `Directory Indexing`은 PDF 표기상 `Directory Listing 취약점`이다. 같은 취약점을 가리키는 표현으로 보되, 노트 제목은 기존 목차 표현을 유지해도 된다.
- `세션과 쿠키`는 기존 목차와 잘 맞지만, raw 메모와 PDF 모두 비중이 크다. 노트 작성 시 단독 중요 노트로 다루는 것이 맞다.
- `웹 인증 구조`는 기존 목차 제목으로는 유지 가능하지만, 실제 노트 제목은 PDF 범위에 맞춰 `기초 웹 인증 방식 - Basic, Anonymous, Form`을 우선 검토한다.
- `ID/PW Brute Forcing`은 공격명으로는 맞지만, 실제 노트 제목은 방어 관점을 살려 `로그인 Brute Force와 계정 보호`가 더 적절하다.

---

## 내일 바로 할 작업 후보

컨디션이 낮으면 새 노트 작성 전에 오늘 만든 노트들의 `확인 질문`만 먼저 풀어본다.

새 노트를 시작한다면 우선순위는 다음과 같다.

1. `로그인 Brute Force와 계정 보호`
   - PDF p.77-79.
   - 기존 [[Hydra 로그인 Brute Force 실습]]이 방어 관점까지 충분히 흡수했는지 먼저 점검한다.

최근 처리된 후보:

- `웹 진단 기초 - Spidering과 Client-side Validation 우회`: [[웹 진단 기초 - Spidering과 Client-side Validation 우회]]
- `OWASP Top 10 2025`: [[OWASP Top 10 2025]]
- `웹 인코딩`: [[HTML 인코딩]]
- `웹 문서, DOM, 웹 언어`: [[웹 문서와 실행 위치]]
- `기초 웹 인증 방식 - Basic, Anonymous, Form`: [[기초 웹 인증 방식 - Basic, Anonymous, Form]]
- `세션과 쿠키`: [[세션과 쿠키]]

---

## 작성 시 주의

- 이 구조 지도는 노트 본문이 아니다. 각 세부 노트 작성 시 해당 page range를 다시 읽는다.
- PDF page는 출처로 남기고, 본문 제목은 개념 중심으로 쓴다.
- raw 메모 질문은 삭제하지 않고 `질문 -> 해석 -> 답`으로 흡수한다.
- 공격 예시는 학습/방어 맥락으로 제한한다.
- 최신성이 필요한 주제는 작성 시점에 공식 문서를 다시 확인한다.
