---
type: moc
status: active
created: "2026-07-10"
scope: note-templates
parent_moc: "[[00_index/Home]]"
---

# 작성 스키마와 선택 템플릿

## 운영 원칙

- 새 노트는 템플릿 없이 작성할 수 있다.
- 먼저 역할과 상위 MOC를 정하고, 최소 frontmatter만 맞춘다.
- 본문 목차와 설명 순서는 자료와 작업 목적에 맞게 정한다.
- 아래 템플릿은 출력 형태가 불명확할 때만 보는 선택 예시다.
- 자동 커밋 전에는 `--changed`, 자동 커밋 후에는 `--range START_REF..END_REF`로 검증한다.

## 선택 예시

| 노트 역할 | 템플릿 | canonical `type` |
|---|---|---|
| 독립 개념 설명 | [[90_템플릿/개념정리_템플릿\|개념정리]] | `concept` |
| 명령·오류·검증이 있는 실습 | [[90_템플릿/실습기록_템플릿\|실습기록]] | `lab` |
| 반복 사용하는 명령 묶음 | [[90_템플릿/명령어정리_템플릿\|명령어정리]] | `command-note` |
| 증상에서 원인·해결을 복구 | [[90_템플릿/트러블슈팅_템플릿\|트러블슈팅]] | `troubleshooting` |
| PDF·강의자료 범위 보존 | [[90_템플릿/Source_Digest_템플릿\|Source Digest]] | `source-digest` |
| 주제 탐색과 재시작 라우팅 | [[90_템플릿/인덱스_MOC_템플릿\|인덱스/MOC]] | `moc` |
| 프로젝트 원문 작업 로그 | [[90_템플릿/프로젝트_RAW_로그_템플릿\|프로젝트 RAW 로그]] | `project-raw-log` |
| 하루 작업 digest | [[90_템플릿/프로젝트_일일_로그_템플릿\|프로젝트 일일 로그]] | `project-daily-log` |
| 안정화된 프로젝트 문서 | [[90_템플릿/프로젝트문서_템플릿\|프로젝트 문서]] | `project-doc` |
| 회의 결정과 액션 | [[90_템플릿/회의록_템플릿\|회의록]] | `meeting` |
| 시험 오답 복구 | [[90_템플릿/오답노트_템플릿\|오답노트]] | `wrong-answer` |
| ACL 정책·검증 설계 | [[90_템플릿/ACL_정책_템플릿\|ACL 정책]] | `security-policy` |

## 최소 Frontmatter 표준

모든 새 노트는 `type`, `status`, `created`를 가진다. 나머지는 역할에 맞을 때만 쓴다.

| 필드 | 용도 |
|---|---|
| `type` | 문서 역할. 진행 상태를 넣지 않는다. |
| `status` | `draft`, `active`, `stable`, `legacy`, `stale`, `archived` 중 하나 |
| `created` | 노트 생성일 |
| `topic` | 학습 주제 |
| `parent_moc` | 학습·자료 노트의 상위 MOC |
| `project`, `project_moc` | 프로젝트와 상위 프로젝트 MOC |
| `source`, `source_pages` | 원자료와 범위 |
| `raw_log`, `source_raw` | 정리의 근거가 된 RAW 로그 |
| `evidence` | 실행 결과·캡처·proof 위치 |
| `environment`, `verified_on` | 재현 환경과 실제 검증일 |
| `reviewed_on` | 내용 검토일. `reviewed`를 status로 쓰지 않는다. |

## 상태 규칙

- `raw`는 `type` 또는 RAW 관계 필드로만 쓰고 status로 쓰지 않는다.
- `complete`는 새 status로 쓰지 않는다. 재사용 가능한 정리본은 `stable`, 종료 기록은 `archived`다.
- `reviewed`는 `reviewed_on`에 날짜로 기록한다.
- `status`를 `태그/상태`로 중복 저장하지 않는다.
- 기존 노트는 전수 소급하지 않는다. 직접 수정하거나 현재 경로에 올릴 때만 점진적으로 정렬한다.
