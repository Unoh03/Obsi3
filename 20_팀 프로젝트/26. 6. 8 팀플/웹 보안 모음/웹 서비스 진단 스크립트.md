# KISA CIIP 2026 — Apache 웹서버 취약점 진단 결과

> [!info] 진단 정보
> 
> - **호스트**: webserv
> - **진단 일시**: 2026-06-17 05:09:46
> - **플랫폼**: Apache (웹서버)
> - **도구**: KISA-CIIP-2026 v1.0.0

---

## 📊 진단 통계

|구분|건수|
|---|---|
|✅ 양호|10|
|❌ 취약|7|
|🔍 수동|5|
|➖ N/A|4|
|**총계**|**26**|
|**양호율**|**38.5%**|

---

## ❌ 취약 항목 (7건)

### WEB-04 · 웹서비스 디렉터리 리스팅 방지 설정

> [!danger] VULNERABLE `apache2.conf` 전역 설정에 `Options Indexes FollowSymLinks` 가 존재함

**진단 명령어**

```bash
grep -r 'Options.*Indexes' /etc/apache2/apache2.conf /etc/apache2/sites-available/ /etc/apache2/conf-available/ 2>/dev/null | grep -v '^\s*#'
```

**결과**

```
/etc/apache2/apache2.conf:    Options Indexes FollowSymLinks
/etc/apache2/apache2.conf:#   Options Indexes FollowSymLinks  ← 주석
```

**조치**

```bash
sudo nano /etc/apache2/apache2.conf
# Options Indexes FollowSymLinks  →  Options -Indexes FollowSymLinks
sudo apachectl configtest && sudo systemctl reload apache2
```
![[Pasted image 20260617142409.png]]
> [!note] 참고 `sites-enabled/` 의 vhost 설정은 이미 `-Indexes` 적용됨. 글로벌 기본값만 수정하면 됨.

---

### WEB-07 · 불필요한 파일 제거

> [!danger] VULNERABLE 웹 디렉터리에서 불필요한 파일 3종이 발견됨 (중복 포함 12건)

**발견 파일**

```
/var/www/html/care/vendor/composer/installed.php
/var/www/html/care/vendor/composer/installed.json
/var/www/html/care/config.example.php
```

**조치**

```bash
# config.example.php — 설정 예시 파일, 삭제 권장
sudo rm /var/www/html/care/config.example.php

# vendor/composer/* — Composer 내부 메타파일, 웹 접근 차단 권장
# (삭제 시 의존성 관리 문제 발생 가능 → 접근 차단 방식 권장)
```

> [!warning] 주의 `vendor/` 디렉터리는 삭제보다 Apache에서 접근 차단하는 게 안전함
> 
> ```apache
> <Directory /var/www/html/care/vendor>
>     Require all denied
> </Directory>
> ```

---

### WEB-08 · .htaccess 오버라이드 제한 (AllowOverride)

> [!danger] VULNERABLE `AllowOverride All` 설정이 존재 — 사용자가 `.htaccess`로 보안 설정 변경 가능

**진단 결과**

```
AllowOverride All  (2건 발견)
```

**조치**

```bash
sudo nano /etc/apache2/sites-available/care.conf
# AllowOverride All  →  AllowOverride None
sudo apachectl configtest && sudo systemctl reload apache2
```
![[Pasted image 20260617144132.png]]
> [!note] 예외 `.htaccess` 를 실제로 사용 중인 기능이 있다면 `AllowOverride AuthConfig` 등 최소 범위로 제한

---

### WEB-11 · 웹서비스 경로 설정

> [!danger] VULNERABLE DocumentRoot가 기본 경로 `/var/www/html` 로 설정됨

**진단 결과**

```
DocumentRoot: /var/www/html
```

**조치 방향**

- 시스템 영역과 분리된 별도 경로로 변경 권장 (예: `/srv/www/care`)
- 운영 환경 변경 부담이 크면 `<Directory>` 블록으로 접근 제어 강화

---

### WEB-12 · 웹서비스 링크 사용 금지 (FollowSymLinks)

> [!danger] VULNERABLE `FollowSymLinks` 활성화 — 심볼릭 링크를 통한 시스템 파일 접근 가능

**진단 결과**

```
Options FollowSymLinks           ← apache2.conf 전역
Options Indexes FollowSymLinks   ← apache2.conf 전역
Options -Indexes +FollowSymLinks ← 각 vhost (3건)
```

**조치**

```bash
sudo nano /etc/apache2/apache2.conf
# Options FollowSymLinks  →  Options SymLinksIfOwnerMatch

sudo nano /etc/apache2/sites-available/care.conf
# Options -Indexes +FollowSymLinks  →  Options -Indexes +SymLinksIfOwnerMatch
```

> [!note] SymLinksIfOwnerMatch vs -FollowSymLinks
> 
> - `-FollowSymLinks`: 모든 심볼릭 링크 차단 (가장 안전, 일부 기능 영향 가능)
> - `SymLinksIfOwnerMatch`: 링크와 실제 파일 소유자 동일할 때만 허용 (절충안)

---

### WEB-20 · SSL/TLS 활성화

> [!danger] VULNERABLE — ⚠️ 진단 스크립트 오탐 의심

**진단 결과 모순**

```
command_result: SSL module loaded, HTTPS configured  ← 양호
Result: VULNERABLE  ← 모순
```

**수동 확인**

```bash
# SSL 모듈 로드 여부
apache2ctl -M | grep ssl

# 443 포트 리스닝 여부
ss -tlnp | grep 443

# SSL vhost 확인
cat /etc/apache2/sites-enabled/care-ssl.conf
```

> [!warning] 오탐 가능성 `sites-enabled/care-ssl.conf` 가 이미 존재하므로 실제로는 HTTPS가 구성된 것으로 보임. 진단 스크립트 로직 버그일 수 있음. 수동 확인 필요.

---

### WEB-26 · 로그 디렉터리 및 파일 권한 설정

> [!danger] VULNERABLE 일부 로그 파일이 644 권한 (타 사용자 읽기 가능)

**진단 결과**

|파일|권한|상태|
|---|---|---|
|access.log|640|✅|
|care_access.log|640|✅|
|care_error.log|640|✅|
|error.log|640|✅|
|care-admin-8888-access.log|**644**|❌|
|care-admin-8888-error.log|**644**|❌|
|care_ssl_access.log|**644**|❌|
|care_ssl_error.log|**644**|❌|
|/var/log/apache2 (디렉터리)|750|✅|

**조치**

```bash
sudo chmod 640 /var/log/apache2/care-admin-8888-access.log
sudo chmod 640 /var/log/apache2/care-admin-8888-error.log
sudo chmod 640 /var/log/apache2/care_ssl_access.log
sudo chmod 640 /var/log/apache2/care_ssl_error.log

# 로테이션된 오래된 파일도 일괄 처리
sudo chmod 640 /var/log/apache2/*.log.*.gz
```

---

## ✅ 양호 항목 (10건)

|항목|내용|
|---|---|
|WEB-05|CGI 실행이 `/cgi-bin/` 으로 제한됨|
|WEB-09|Apache 자식 프로세스가 `www-data` 계정으로 구동|
|WEB-10|불필요한 Proxy 설정 없음|
|WEB-14|적절한 접근 제어 설정 (`Require all denied/granted`)|
|WEB-15|불필요한 스크립트 매핑 없음|
|WEB-16|`ServerTokens Prod`, `ServerSignature Off` 설정됨|
|WEB-17|불필요한 가상 디렉터리(Alias) 없음|
|WEB-18|WebDAV 비활성화|
|WEB-19|WebDAV 모듈 미로드|
|WEB-22|커스텀 에러 페이지 설정됨 (400/403/404/500)|

---

## 🔍 수동 진단 항목 (5건)

|항목|내용|확인 방법|
|---|---|---|
|WEB-06|상위 디렉터리 접근 제한|`AllowOverride None` 설정 확인, 403 응답 테스트|
|WEB-21|동적 페이지 입력값 검증|소스코드 레벨 수동 검토 필요|
|WEB-23|HTTP 메서드 제한|`LimitExcept` 설정 확인|
|WEB-24|X-Frame-Options 헤더|`Header always set X-Frame-Options DENY` 설정 여부|
|WEB-25|X-XSS-Protection 헤더|`Header always set X-XSS-Protection "1; mode=block"` 설정 여부|

---

## ➖ N/A 항목 (4건)

|항목|사유|
|---|---|
|WEB-01|Tomcat/IIS/JEUS 대상 항목 — Apache 해당 없음|
|WEB-02|Tomcat/IIS/JEUS 대상 항목 — Apache 해당 없음|
|WEB-03|Tomcat/IIS/JEUS 대상 항목 — Apache 해당 없음|
|WEB-13|WEB-04와 중복 — Apache 해당 없음|

---

## 🛠️ 조치 우선순위

```
즉시 조치 (설정 변경만 필요)
├── WEB-04  apache2.conf Options -Indexes 적용
├── WEB-26  로그 파일 chmod 640
└── WEB-08  AllowOverride None 적용

검토 후 조치 (기능 영향 확인 필요)
├── WEB-12  FollowSymLinks → SymLinksIfOwnerMatch
├── WEB-07  vendor/ 접근 차단, config.example.php 삭제
└── WEB-20  SSL 수동 확인 (오탐 가능성)

장기 검토
└── WEB-11  DocumentRoot 경로 분리
```

---

_출처: KISA-CIIP-2026 v1.0.0 / https://github.com/rebugui/KISA-CIIP-2026_ _진단일: 2026-06-17_