# Wazuh 설치와 AWS 로그 수집 연동

## 설치 목표

프로젝트의 AWS 로그를 한곳에서 검색·분석하고 탐지 규칙을 적용하기 위해 로컬 Docker 환경에 Wazuh 중앙 구성요소를 설치한다.

이번 단계에서는 고가용성 구성이 아닌 **Single-node stack**을 사용한다. 한 대의 Docker 호스트에서 다음 세 구성요소를 각각의 컨테이너로 실행하는 방식이다.

- **Wazuh Manager**: 수집된 이벤트를 분석하고 탐지 규칙을 적용한다.
- **Wazuh Indexer**: 이벤트와 Alert를 저장하고 검색할 수 있게 색인한다.
- **Wazuh Dashboard**: 검색·분석·시각화를 제공하는 웹 화면이다.

AWS 로그 연결과 Shuffle SOAR 연동은 Wazuh 자체의 기동과 데이터 보존을 먼저 확인한 뒤 진행한다.

> [!note] 문서 경계
> 1~7장은 로컬에서 Wazuh를 실행하기 위한 설치 과정이다. 8~9장은 단순 준비를 넘어 CloudTrail을 처음 연결한 과정이고, 10장부터 WAF·DVWA·ALB·CloudFront로 수집 범위를 확장한다. 실제 Event 해석부터 탐지 규칙과 대응 Workflow를 만드는 작업은 [[Wazuh SIEM 로그 분석과 대응]]에서 계속한다.

## 1. 공식 설치 방식 선택

Wazuh 설치 페이지에서 `Installation guide`로 이동했다.

![[Pasted image 20260812164915.png]]

`Installation alternatives → Deployment on Docker → Wazuh Docker deployment`를 선택했다. 프로젝트용 로컬 검증 환경이므로 여러 서버에 구성요소를 분산하는 Multi-node가 아니라, 한 호스트에서 중앙 구성요소 세 개를 실행하는 Single-node 절차를 따랐다.

![[Pasted image 20260812165046.png]]

## 2. 공식 Docker 구성 다운로드

```bash
git clone https://github.com/wazuh/wazuh-docker.git -b v4.14.7
```

- 설치 위치: `D:\Wazuh\wazuh-docker`
- 고정 버전: `v4.14.7`
- 확인된 체크아웃 커밋: `adcc5b5`

버전 태그를 직접 체크아웃했기 때문에 나타나는 `detached HEAD`는 정상이다. 애플리케이션 소스를 개발하려는 것이 아니라 동일한 Wazuh 구성을 재현하기 위해 특정 버전에 고정한 상태다.

출력 중 `refs/tags/v4.14.7 ... is not a commit` 경고가 있었지만, Git이 태그를 실제 공식 릴리스 커밋 `adcc5b5`로 해석하여 체크아웃을 완료했으므로 clone 실패로 판정하지 않았다.

![[Pasted image 20260812170714.png]]

## 3. 내부 통신용 인증서 생성

`single-node` 디렉터리에서 다음 명령을 실행했다.

```bash
cd wazuh-docker/single-node
docker compose -f generate-indexer-certs.yml run --rm generator
```

명령의 의미는 다음과 같다.

- `-f generate-indexer-certs.yml`: 기본 Compose 파일 대신 인증서 생성 전용 설정을 사용한다.
- `run generator`: 인증서 생성 서비스를 일회성으로 실행한다.
- `--rm`: 작업이 끝난 임시 컨테이너만 삭제한다. 호스트 폴더에 생성된 인증서는 남는다.

![[Pasted image 20260812171713.png]]

생성기는 Manager·Indexer·Dashboard 및 Filebeat가 서로 TLS로 통신할 때 사용할 인증서를 만들었다.

![[Pasted image 20260812171726.png]]

### 인증서 생성 결과 판정

출력에 `/wazuh-certs-tool.sh: line 636: find: command not found`가 표시됐다. 그러나 다음 경로에 인증서와 키 12개가 실제로 생성된 것을 확인했다.

```text
D:\Wazuh\wazuh-docker\single-node\config\wazuh_indexer_ssl_certs\
```

이후 세 중앙 구성요소가 해당 인증서를 사용해 정상 기동하고 Dashboard HTTPS 접속까지 성공했다. 따라서 이번 실행에서는 인증서 생성 결과를 유효한 것으로 판정하되, 재현 시 참고할 수 있도록 경고 문구는 기록으로 남긴다.

## 4. Wazuh Single-node 기동

```bash
docker compose up -d
```

Docker Compose가 Wazuh Manager·Indexer·Dashboard `v4.14.7` 이미지를 내려받고, 구성에 필요한 Network·영속 Volume·컨테이너를 생성한 뒤 백그라운드에서 실행했다.

![[Pasted image 20260812175513.png]]

이 명령은 공식 `docker-compose.yml`에 정의된 포트, 환경변수, 인증서 Mount, Network, Volume과 구성요소 간 연결 관계를 한꺼번에 적용한다. 개별 이미지를 Docker Desktop에서 따로 실행하지 않은 이유는 이 관계를 GUI에서 모두 수동으로 재현하면 누락 가능성이 커지기 때문이다.

## 5. Docker Desktop에서 실행 상태 확인

Docker Desktop의 `Containers` 화면에서 세 구성요소가 `single-node` Compose 애플리케이션으로 묶여 실행되는 것을 확인했다.

![[Pasted image 20260812180130.png]]

그룹을 펼쳐 Manager·Indexer·Dashboard의 실행 상태와 통합 로그를 확인했다. Indexer 로그에서는 Cluster Health가 `YELLOW`에서 `GREEN`으로 전환됐다.

![[Pasted image 20260812180843.png]]

초기 로그에는 구성요소 시작 순서로 인한 `ECONNREFUSED`와 이미 생성된 Index를 다시 만들면서 발생한 `resource_already_exists_exception`이 보였다. 그러나 이후 다음 근거가 연속으로 확인됐으므로 이번 실행의 치명적인 기동 실패로 판정하지 않았다.

- 세 컨테이너가 모두 계속 `Up` 상태를 유지했다.
- Indexer Cluster Health가 `GREEN`으로 전환됐다.
- Dashboard가 `https://0.0.0.0:5601`에서 실행됐다는 로그가 남았다.
- 브라우저에서 실제 로그인과 Dashboard 접속에 성공했다.

각 컨테이너의 호스트 포트 연결도 확인했다.

- **Indexer**: `9200:9200`
- **Manager**: `1514`, `1515`, `514/UDP`, `55000`
- **Dashboard**: 호스트 `443` → 컨테이너 `5601`

![[Pasted image 20260812181607.png]]

## 6. HTTPS 접속과 로그인

Docker Desktop의 포트 링크는 `http://localhost:443`으로 열려 `ERR_EMPTY_RESPONSE`가 발생했다. Dashboard 장애가 아니라 HTTPS 서비스에 HTTP로 접근한 것이 원인이었다.

다음 주소를 직접 입력해 로그인 화면에 접속했다.

```text
https://localhost
```

Wazuh가 생성한 자체 서명 인증서를 사용하므로 브라우저에 인증서 경고가 표시된다. 로컬 검증 환경임을 확인한 뒤 접속을 계속했다.

![[Pasted image 20260812182212.png]]

최초 로그인에는 공식 Docker 구성의 기본 관리자 계정을 사용했다.

- 초기 사용자: `admin`
- 초기 비밀번호: 공식 Docker 기본값 사용

> [!warning]
> 기본 비밀번호는 최초 접속을 위한 값이다. 외부에 노출되는 운영 환경에서는 공식 절차에 따라 변경해야 하며, 변경한 실제 비밀번호는 Obsidian·Git·스크린샷에 기록하지 않는다.

## 7. Dashboard 접속 확인

로그인 후 Wazuh Overview 화면에 접근했다. 이를 통해 Docker에서 실행된 Dashboard가 HTTPS 요청에 정상 응답하고, Dashboard·Indexer·Manager 간 연결이 실제 웹 UI 사용까지 이어지는 것을 확인했다.

![[Pasted image 20260812182642.png]]

화면에 `This instance has no agents registered`가 표시되는 것은 현재 단계에서 정상이다. 이번 프로젝트는 먼저 Wazuh Agent를 PC에 배포하는 것이 아니라, Wazuh Manager가 AWS 로그 원본을 읽도록 연결할 계획이다.

또한 초기 화면의 Alert 개수와 `Amazon Web Services` 메뉴는 Wazuh의 기본 화면 및 기능을 보여줄 뿐이다. **아직 프로젝트의 AWS 로그가 수집됐다는 증거는 아니다.** AWS 연동 완료 판정은 CloudTrail 등 실제 프로젝트 이벤트가 Wazuh에서 검색되고 원본 이벤트까지 추적되는지를 별도로 검증한 뒤 내린다.

![[Pasted image 20260812183713.png]]

## 8. AWS 로그 읽기용 최소 권한 Policy 작성

AWS Console의 `IAM → Policies → Create policy → JSON`에서 로컬 Wazuh 전용 Reader가 사용할 고객 관리형 Policy `aws-topology-wazuh-local-reader-policy`를 생성했다.

이 Policy의 의미는 다음과 같다.

- 최초 Policy에서는 CloudTrail Prefix만 목록 조회·Object 읽기를 허용했다. 이후 Wazuh의 Bucket 최상위 사전 확인 동작에 맞춰 **Bucket Key 목록 조회만 전체 허용**하고, 실제 Object 내용 읽기는 계속 CloudTrail Prefix로 제한했다.
- CloudWatch Logs는 **WAF와 Primary DVWA Log Group만** 조회할 수 있다.
- 로그 쓰기·수정·삭제와 다른 AWS Resource 접근은 허용하지 않는다.
- 첫 Runtime 검증에서는 CloudTrail만 연결하고, WAF·DVWA 권한은 이후 수집 범위를 확장할 때 사용한다.

> [!code]- 최초 적용한 IAM Policy JSON 전문
> ```json
> {
>   "Version": "2012-10-17",
>   "Statement": [
>     {
>       "Sid": "ListCloudTrailPrefix",
>       "Effect": "Allow",
>       "Action": "s3:ListBucket",
>       "Resource": "arn:aws:s3:::aws-topology-security-logs-e10b7e4f152e9420159dba755d",
>       "Condition": {
>         "StringLike": {
>           "s3:prefix": [
>             "AWSLogs/433048100798/CloudTrail",
>             "AWSLogs/433048100798/CloudTrail/*"
>           ]
>         }
>       }
>     },
>     {
>       "Sid": "ReadCloudTrailObjects",
>       "Effect": "Allow",
>       "Action": "s3:GetObject",
>       "Resource": "arn:aws:s3:::aws-topology-security-logs-e10b7e4f152e9420159dba755d/AWSLogs/433048100798/CloudTrail/*"
>     },
>     {
>       "Sid": "DescribeApprovedLogStreams",
>       "Effect": "Allow",
>       "Action": "logs:DescribeLogStreams",
>       "Resource": [
>         "arn:aws:logs:us-east-1:433048100798:log-group:aws-waf-logs-aws-topology-edge:*",
>         "arn:aws:logs:ap-northeast-2:433048100798:log-group:/aws/eks/aws-topology-primary/dvwa:*"
>       ]
>     },
>     {
>       "Sid": "ReadApprovedLogEvents",
>       "Effect": "Allow",
>       "Action": "logs:GetLogEvents",
>       "Resource": [
>         "arn:aws:logs:us-east-1:433048100798:log-group:aws-waf-logs-aws-topology-edge:log-stream:*",
>         "arn:aws:logs:ap-northeast-2:433048100798:log-group:/aws/eks/aws-topology-primary/dvwa:log-stream:*"
>       ]
>     }
>   ]
> }
> ```

생성 결과 화면에서 허용 서비스가 CloudWatch Logs와 S3 두 개이며, 액세스 수준이 나열·읽기로 제한된 것을 확인했다.

![[Pasted image 20260812200746.png]]

### 전용 IAM User 생성과 Policy 연결

로컬 Wazuh만 사용할 IAM User `aws-topology-wazuh-local-reader`를 생성하고, AWS Console 로그인은 허용하지 않았다. 생성 검토 화면에서 연결된 권한이 앞서 만든 고객 관리형 Policy 하나뿐인 것을 확인했다.

![[Pasted image 20260812201204.png]]

생성된 User의 권한 화면에서도 `aws-topology-wazuh-local-reader-policy` 하나가 직접 연결된 것을 다시 확인했다.

![[Pasted image 20260812201356.png]]

### 장기 Credential Profile 인증 확인

로컬 Docker Wazuh가 반복적인 STS Session 갱신 없이 AWS 로그를 읽도록 전용 IAM User의 Access Key를 생성했다. 실제 Access Key와 Secret Access Key는 Obsidian·Git·Screenshot에 기록하지 않고, Git 밖의 전용 AWS Credential 파일에 `wazuh-reader` Profile로 저장했다.

> [!warning]
> 장기 Access Key 사용은 로컬 실습 편의를 위한 프로젝트 한정 절충안이다. `terra-user` Credential을 재사용하지 않고 읽기 권한만 가진 전용 User로 범위를 제한했으며, 프로젝트 종료 후 Access Key를 비활성화·삭제한다.

`aws sts get-caller-identity` 결과가 전용 IAM User ARN을 반환해 `wazuh-reader` Profile의 인증을 확인했다.

![[Pasted image 20260812202500.png]]

### 실제 CloudTrail Object 읽기 검증

첫 검증에서는 S3 Prefix와 동일한 0바이트 폴더 표시용 Object를 선택했다. 이 결과는 목록 조회와 해당 빈 Object 접근만 증명하므로 실제 CloudTrail 로그 읽기의 최종 Evidence로 사용하지 않았다.

> [!note]- 처음 수행한 0바이트 Object 검증
> 반환된 Key가 `AWSLogs/433048100798/CloudTrail/`에서 끝나고 `Size`가 `0`이므로 실제 CloudTrail 로그 파일이 아니다.
>
> ![[Pasted image 20260812203005.png]]

검증 대상을 `ap-northeast-2` 경로의 최신 `*.json.gz` 파일로 보정했다. 실제 CloudTrail Object Key가 출력됐고 `head-object`가 `Size: 6837`을 반환했다. 이를 통해 전용 Reader의 CloudTrail Prefix `ListBucket`과 실제 Object `GetObject` 권한이 Runtime에서 모두 작동함을 확인했다. Object 내용은 내려받거나 수정하지 않았다.

![[Pasted image 20260812203308.png]]

### Credential 파일을 Wazuh Manager에 Read-only로 연결

Git 밖에 저장한 `wazuh-reader` Credential 파일을 Wazuh Manager의 `/root/.aws/credentials`에 Bind Mount하도록 `docker-compose.yml`의 `wazuh.manager.volumes`에 설정을 추가했다. `read_only: true`로 Container의 쓰기를 막고, `create_host_path: false`로 Host 파일이 없을 때 Docker가 같은 이름의 디렉터리를 자동 생성하지 않게 했다.

![[Pasted image 20260812204207.png]]

변경 사항을 적용하기 위해 전체 Stack이 아니라 Wazuh Manager 하나만 재생성했다. `--no-deps`로 Indexer·Dashboard 재생성을 막았으며 기존 Named Volume은 삭제하지 않았다.

```powershell
docker compose up -d --no-deps --force-recreate wazuh.manager
```

![[Pasted image 20260812204438.png]]

Manager 재생성 뒤 `docker inspect`로 실제 Container Mount를 확인했다. 출력의 `RW=false`와 `Type=bind`는 Credential 파일이 Bind Mount로 연결됐고 Container에서 쓸 수 없음을 의미한다. 이어서 파일 내용을 출력하지 않고 `wazuh-reader` Profile Header가 읽히는지만 확인했다.

![[Pasted image 20260812204718.png]]

## 9. Wazuh AWS 모듈에 CloudTrail 수집 설정

### Docker 환경의 설정 원본 선택

Wazuh Dashboard의 `Server management → Settings`에서 실행 중인 Manager 설정을 확인했다.

![[Pasted image 20260813101246.png]]

![[Pasted image 20260813101739.png]]
![[Pasted image 20260813103052.png]]

마지막 화면은 Dashboard에서 실행 중인 Wazuh Manager의 `ossec.conf`를 확인·편집하는 화면이다. 다만 현재 환경은 공식 Docker Compose 구성으로, 호스트의 `D:\Wazuh\wazuh-docker\single-node\config\wazuh_cluster\wazuh_manager.conf`가 컨테이너의 `/wazuh-config-mount/etc/ossec.conf`에 Mount된다.

따라서 Dashboard 화면은 설정 구조를 확인하는 용도로만 사용하고, 재생성 후에도 남아야 하는 CloudTrail 수집 설정은 호스트 파일에 기록했다. 이렇게 해야 Manager Container를 재생성해도 같은 설정이 다시 적용되고, 설정 원본도 명확하게 관리된다. Dashboard에서는 `Save`나 `Restart Manager`를 누르지 않았다. ([Wazuh Docker 공식 문서](https://documentation.wazuh.com/current/deployment-options/docker/container-usage.html))

### CloudTrail 수집 블록 적용

호스트의 `wazuh_manager.conf`에 다음 범위의 설정을 추가했다.

- Source: Security Log S3 Bucket의 CloudTrail
- 인증: Read-only로 Mount한 `wazuh-reader` Profile
- 범위: AWS Account `433048100798`, `ap-northeast-2`
- 최초 수집 기준: `2026-AUG-12`
- 실행: Manager 시작 직후 한 번, 이후 10분 간격
- 원본 보존: 수집 후에도 S3 Object를 삭제하지 않음

![[Pasted image 20260813104207.png]]

전체 Stack이 아니라 Wazuh Manager만 재생성해 호스트 설정을 적용했다. `--no-deps`로 Indexer와 Dashboard 재생성을 막았고 Named Volume은 삭제하지 않았다.

```powershell
docker compose up -d --no-deps --force-recreate wazuh.manager
```

![[Pasted image 20260813103949.png]]

이후 Wodle 전용 설정 검사 명령을 실행했다.

```powershell
docker compose exec wazuh.manager /var/ossec/bin/wazuh-modulesd -t
$LASTEXITCODE
```

종료 코드가 `0`이었으므로 Wazuh가 `aws-s3` 블록을 유효한 설정으로 받아들였음을 확인했다. 이는 **설정 문법과 지원 항목이 정상이라는 증거**이며, AWS 인증·권한과 실제 로그 수집 성공까지 증명하는 결과는 아니다.

### 최초 Runtime 수집 실패

설정 적용 직후 AWS 모듈은 시작됐지만 S3 수집은 다음 상태로 종료됐다.

```text
Module AWS started
→ Starting fetching of logs
→ Bucket Analysis 시작
→ exit code 1 / Unknown error
```

`Unknown error`만으로는 Credential·권한·경로 중 무엇이 문제인지 구분할 수 없었다. 같은 조건으로 Wazuh의 `aws-s3` 수집기를 `--debug 2`로 직접 실행하자 실제 원인이 드러났다.

```text
AccessDenied: s3:ListBucket
```

### 정확한 원인: 직접 검증과 Wazuh 실행 순서의 차이

최초 IAM Policy는 `s3:ListBucket`을 다음 CloudTrail Prefix로 제한했다.

```text
AWSLogs/433048100798/CloudTrail
AWSLogs/433048100798/CloudTrail/*
```

전날 AWS CLI 검증은 `AWSLogs/433048100798/CloudTrail/ap-northeast-2/`를 직접 지정했기 때문에 이 조건과 일치하여 성공했다. 이 검증으로 Credential과 해당 Prefix의 목록 조회, CloudTrail Object 접근은 확인했지만 **Wazuh가 실행하는 전체 API 순서까지는 검증하지 못했다.**

설치된 Wazuh `v4.14.7`의 `AWSBucket.check_bucket()` 동작을 확인한 결과, 실제 수집 전에 다음 순서로 접근했다.

```text
1. ListObjectsV2(Prefix="", Delimiter="/")
   → Bucket이 열리는지와 비어 있지 않은지 사전 확인

2. ListObjectsV2(Prefix="AWSLogs/.../CloudTrail/ap-northeast-2/")
   → 대상 CloudTrail Object 목록 조회

3. GetObject
   → CloudTrail 로그를 읽어 Wazuh 규칙으로 분석
```

첫 번째 요청의 Prefix는 빈 값이므로 최초 Policy의 CloudTrail Prefix 조건과 일치하지 않았다. IAM에서 조건이 일치하지 않는 `Allow` 문장은 권한이 없는 것과 동일하게 평가되므로, AWS는 `no identity-based policy allows the s3:ListBucket action`을 반환했다.

즉 실패 원인은 Access Key·Secret Key, XML 문법, Bucket 이름이 아니라 **Wazuh의 Bucket 최상위 사전 확인을 최초 IAM Policy가 허용하지 않은 것**이었다.

### IAM Policy 보정 원리

Wazuh 공식 CloudTrail 연동이 요구하는 동작에 맞춰 `s3:ListBucket`의 Prefix 조건만 제거했다. 최종 목록 조회 문장은 다음과 같다.

```json
{
  "Sid": "ListSecurityLogBucketForWazuh",
  "Effect": "Allow",
  "Action": "s3:ListBucket",
  "Resource": "arn:aws:s3:::aws-topology-security-logs-e10b7e4f152e9420159dba755d"
}
```

권한 보정의 범위는 다음과 같다.

- 허용 확대: Security Log Bucket의 Object Key 목록 조회
- 그대로 제한: 실제 Object 내용은 `AWSLogs/433048100798/CloudTrail/*`만 `GetObject`
- 계속 금지: S3 쓰기·수정·삭제
- 변경 없음: 기존에 승인한 CloudWatch Log Group 읽기 권한 외의 AWS 권한은 추가하지 않음

따라서 Wazuh는 Bucket 최상위를 확인할 수 있지만, WAF·ALB·VPC Flow Log 등 아직 승인하지 않은 Object의 내용까지 읽을 수는 없다.

### 보정 후 재검증

동일한 Debug 명령을 다시 실행하자 `AccessDenied`가 사라지고 다음 단계까지 진행됐다.

```text
Working on 433048100798 - ap-northeast-2
→ Marker: ...20260813T0200Z_....json.gz
→ No logs to process
→ DB Maintenance
```

![[Pasted image 20260813110235.png]]

`Marker`가 날짜 기준점이 아니라 실제 CloudTrail `.json.gz` Object Key라는 것은 Wazuh 내부 수집 DB에 해당 파일이 마지막 처리 지점으로 기록됐다는 의미다. Policy 보정 후 10분 주기의 백그라운드 수집이 먼저 실행되어 해당 Object까지 처리한 것으로 판단한다.

`No logs to process`는 Bucket에 로그가 없거나 수집에 실패했다는 뜻이 아니다. **기록된 Marker보다 새로운 Object가 아직 없어서 이번 수동 실행에서 추가 처리할 파일이 없었다는 뜻**이다.
![[Pasted image 20260813110857.png]]
![[Pasted image 20260813111309.png]]

## 10. WAF·DVWA CloudWatch Logs 수집 확장

CloudTrail은 Security Log S3에서 Object를 읽지만, WAF와 DVWA는 이미 CloudWatch Logs에
기록되고 있다. 따라서 같은 `aws-s3` Wodle 안에서도 입력 방식이 다르다.

| Source | 원본 위치 | Wazuh 입력 방식 | Region |
|---|---|---|---|
| CloudTrail | Security Log S3 | `<bucket type="cloudtrail">` | `ap-northeast-2` |
| WAF | `aws-waf-logs-aws-topology-edge` | `<service type="cloudwatchlogs">` | `us-east-1` |
| DVWA | `/aws/eks/aws-topology-primary/dvwa` | `<service type="cloudwatchlogs">` | `ap-northeast-2` |

WAF는 CloudFront와 같은 글로벌 Edge 경로에 있어 `us-east-1`, DVWA Pod Log는 서울 EKS에
있어 `ap-northeast-2`다. 두 Log Group을 한 덩어리로 적지 않고 별도 `service` Block으로
나눈 이유다.

설정은 Container 내부에서 임시로 고친 것이 아니라 다음 Host 원본 파일에 저장했다.

```text
D:\Wazuh\wazuh-docker\single-node\config\wazuh_cluster\wazuh_manager.conf
```

두 Block은 모두 `aws_profile=wazuh-reader`, `only_logs_after=2026-AUG-12`,
`remove_log_streams=no`를 사용한다. `run_on_start=yes`는 **PC 부팅 시 Container를 자동으로
켠다는 뜻이 아니라**, Wazuh Manager가 시작될 때 AWS 수집 작업을 한 번 바로 실행한다는
뜻이다.

Manager만 재생성한 뒤 설정 검사를 수행했다.

```powershell
docker compose up -d --no-deps --force-recreate wazuh.manager
docker compose exec wazuh.manager /var/ossec/bin/wazuh-modulesd -t
$LASTEXITCODE
```

검사 결과는 Exit Code `0`이었다. `ossec.log`에는 두 Source에 대한
`Executing Service Analysis: cloudwatchlogs`와 정상 종료가 기록됐고,
`AccessDenied`, `ExpiredToken`, `Unknown error`는 나타나지 않았다.

### 실제 Archive 수집 확인

연결 직후 Manager의 Raw Archive에서 단순 시작 로그가 아니라 실제 Source Record를
확인했다.

| Source | 확인 결과 | 해석 |
|---|---:|---|
| WAF | 실제 요청 Record 4건 | `action=ALLOW`, HTTP Method, CloudFront Source 등이 파싱됨 |
| DVWA | Pod Record 13,817건 | `dvwa-*` Pod의 stdout·PHP Warning 등을 과거 시점부터 Backfill함 |

DVWA의 `13,817건`은 공격이 13,817번 있었다는 뜻이 아니다. 일반 애플리케이션 출력과
Warning까지 포함한 **Raw Event 양**이다. 이 결과는 로그 수집 성공을 증명하는 동시에,
그대로는 사람이 읽기 어려우므로 Rule·Filter·Dashboard가 필요하다는 점도 보여준다.

현재 Runtime으로 확인된 수집 경로는 다음과 같다.

```text
CloudTrail S3 ─┐
WAF Logs ──────┼→ Local Wazuh Manager → Raw Archive
DVWA Pod Logs ─┘
```

이 단계는 **수집 성공**이다. WAF·DVWA Event를 프로젝트 사건으로 판정하는 Custom Rule과
초보자용 화면까지 완성했다는 뜻은 아니다.

## 11. ALB·CloudFront 연결 방식 조사

Terraform과 실제 Evidence를 대조하면 두 Source 모두 기존 Security Log S3에 저장되지만,
형식과 Wazuh 지원 방식은 서로 다르다.

| Source | 현재 저장 경로 | Wazuh 연결 판단 | 현재 상태 |
|---|---|---|---|
| ALB | `alb/primary/AWSLogs/.../elasticloadbalancing/ap-northeast-2/` | 공식 `<bucket type="alb">`와 `path` 지원 | IAM `GetObject` 범위 저장, AWS 재조회·Wazuh Runtime 검증 전 |
| CloudFront | `AWSLogs/.../CloudFront/` | 현재 JSON은 설치된 Wazuh `custom` Bucket Parser가 기대하는 `detail` 구조가 아님 | S3 보존 중, Wazuh 직접 연결 안 함 |

ALB는 다음 단계에서 기존 Bucket과 `alb/primary` Prefix를 지정하면 된다. Reader Policy에는
해당 Prefix의 `s3:GetObject`를 추가해 콘솔에서 저장했지만, **실제 AWS Policy 재조회와
Wazuh 수집 성공은 아직 검증하지 않았다.**

CloudFront는 S3 Object가 존재한다고 해서 `<bucket type="custom">`으로 바로 읽을 수 있는
것이 아니다. 설치된 Wazuh `v4.14.7` 구현을 확인한 결과 Custom Bucket Loader는 `detail`
필드를 가진 JSON Event를 처리하는 경로인데, 현재 CloudFront Standard Logging v2 JSON에는
그 구조가 없다. 잘못 연결하면 오류 없이 Event가 0건인 것처럼 보일 수 있다.

따라서 CloudFront는 기존 S3 Evidence 보존을 유지하고, 필요하면 **CloudWatch Logs를 병렬
Destination으로 추가한 뒤 Wazuh가 그 Log Group만 읽는 방식**을 후보로 둔다. AWS는 여러
Destination을 지원하지만 CloudWatch Logs 전달·수집·보존 비용이 생기므로 Toggle과 짧은
Retention을 먼저 설계해야 한다. 아직 구현한 상태는 아니다.

## 현재 판정

**CloudTrail·WAF·DVWA 세 Source가 Local Wazuh Raw Archive까지 들어오는 경로를 Runtime으로 확인했다.** CloudTrail은 Custom Rule `100100` Alert까지 확인했지만, WAF·DVWA는 아직 Raw 수집 단계다. ALB는 공식 지원 방식과 필요한 IAM 범위를 확인했고, CloudFront는 현재 S3 JSON을 Wazuh에 직접 연결하지 않기로 판단했다.

- [x] 공식 Docker 구성 `v4.14.7` 다운로드
- [x] Single-node용 인증서 파일 생성 확인
- [x] Wazuh Manager·Indexer·Dashboard 실행
- [x] 세 컨테이너의 실행 상태와 로그 확인
- [x] Dashboard HTTPS 접속 및 로그인 확인
- [x] AWS 로그 Source용 최소 권한 Reader Policy 생성
- [x] 전용 IAM User 생성과 Policy 연결
- [x] 장기 `wazuh-reader` Profile 인증 확인
- [x] 실제 CloudTrail `*.json.gz` Object 읽기 권한 확인
- [x] Credential 파일을 Wazuh Manager에 Read-only로 Mount
- [x] Wazuh AWS 모듈에 CloudTrail 수집 설정 적용
- [x] `wazuh-modulesd -t` 설정 검사 통과
- [x] Wazuh Runtime의 `ListBucket` 요구사항에 맞게 Reader Policy 보정
- [x] CloudTrail 실제 Object Marker와 정상 종료 확인
- [x] Wazuh Dashboard에서 CloudTrail Alert의 Source·Account·Bucket·Region 집계 확인
- [x] WAF CloudWatch Log Group 연결과 실제 요청 Record 확인
- [x] DVWA CloudWatch Log Group 연결과 실제 Pod Record 확인
- [x] WAF·DVWA 설정 검사와 Runtime 오류 없음 확인
- [ ] ALB Reader Policy의 AWS 저장 상태 재조회
- [ ] Wazuh `<bucket type="alb">` 적용과 실제 ALB Record 확인
- [ ] CloudFront 병렬 CloudWatch Logs Destination의 비용·Retention 설계
- [ ] CloudFront Log Group 연결과 실제 Record 확인
- [ ] 기본 비밀번호 변경
- [ ] 재시작 후 Volume과 데이터 보존 확인

## 다음 문서

- SIEM 활용: [[Wazuh SIEM 로그 분석과 대응]]
- 설치 환경에서 남은 운영 점검: 기본 비밀번호 변경, Docker 재시작 후 Credential Mount와 Wazuh 데이터 보존 확인

> [!danger]
> Docker Desktop의 정지·시작은 컨테이너와 Volume을 삭제하지 않는다. 반면 `docker compose down -v` 또는 Volume 삭제는 Wazuh의 저장 데이터를 제거할 수 있으므로 초기화를 의도한 경우가 아니면 사용하지 않는다.

## 참고

- [Wazuh Docker deployment](https://documentation.wazuh.com/current/deployment-options/docker/wazuh-container.html)
- [Wazuh Docker container usage](https://documentation.wazuh.com/current/deployment-options/docker/container-usage.html)
- [Wazuh AWS CloudTrail](https://documentation.wazuh.com/current/cloud-security/amazon/services/supported-services/cloudtrail.html)
- [Wazuh AWS CloudWatch Logs](https://documentation.wazuh.com/current/cloud-security/amazon/services/supported-services/cloudwatchlogs.html)
- [Wazuh AWS Application Load Balancer](https://documentation.wazuh.com/current/cloud-security/amazon/services/supported-services/elastic-load-balancing/alb.html)
- [Amazon CloudFront Standard Logging](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/standard-logging.html)
- [Wazuh AWS troubleshooting](https://documentation.wazuh.com/current/cloud-security/amazon/services/troubleshooting.html)
- [Wazuh configuration verification](https://documentation.wazuh.com/current/user-manual/reference/ossec-conf/verifying-configuration.html)
- [Changing the default password of Wazuh users](https://documentation.wazuh.com/current/deployment-options/docker/changing-default-password.html)
