---
type: project-doc
status: draft
study_status: not-started
created: 2026-08-04
project: 3차 프로젝트
project_moc: "[[20_팀 프로젝트/3차 프로젝트/00_3차프로젝트_목차]]"
---

# 로그 인프라 전체 흐름

> [!summary] 이 문서의 역할
> 프로젝트의 로그 생성, 전달, 저장, 분석, 탐지, 알림, 증거화 흐름을 한 장으로 정리한 공통 지도다.  
> 각 서비스의 상세 원리는 개별 학습 노트에서 다룬다.

## 전체 흐름

```mermaid
flowchart TB
    subgraph REQUEST["외부 요청 처리 경로"]
        User["사용자·실습자"] --> Route53["Amazon Route 53"]
        Route53 --> Edge["Edge Boundary<br/>CloudFront + WAF"]
        Edge --> ALB["Application Load Balancer"]
        ALB --> K8sService["TargetGroupBinding<br/>Kubernetes Service"]
        K8sService --> DVWA["DVWA Pod"]
        DVWA --> RDS["Amazon RDS"]
    end

    subgraph PRODUCER["로그·Finding 생성"]
        CFLog["CloudFront Access Log"]
        WAFLog["WAF COUNT·BLOCK Log"]
        ALBLog["ALB Access Log"]
        AppLog["DVWA Audit·Container Log"]
        CTLog["CloudTrail Event"]
        EKSLog["EKS Control Plane Log"]
        FlowLog["VPC REJECT Flow Log"]
        GDFinding["GuardDuty Finding"]
    end

    Edge -. "Edge 요청·응답" .-> CFLog
    Edge -. "Rule Match·Action" .-> WAFLog
    ALB -. "Target 전달 결과" .-> ALBLog
    DVWA -. "Application 의미" .-> AppLog

    AWSAPI["Console·CLI·Terraform·AWS Service<br/>AWS API 호출"] --> CTLog
    K8sAPI["kubectl·Controller<br/>Kubernetes API 호출"] --> EKSLog
    Network["VPC ENI의 거부된 Traffic"] --> FlowLog
    ThreatSignal["AWS 위협 신호"] --> GDFinding

    subgraph DELIVERY["수집·전달"]
        CFDelivery["CloudFront Standard Logging v2"]
        AWSDelivery["AWS Service Log Delivery"]
        FluentBit["Fluent Bit DaemonSet"]
        EventBridge["Amazon EventBridge"]
    end

    CFLog --> CFDelivery
    WAFLog --> AWSDelivery
    ALBLog --> AWSDelivery
    AppLog --> FluentBit
    CTLog --> AWSDelivery
    EKSLog --> AWSDelivery
    FlowLog --> AWSDelivery
    GDFinding --> EventBridge

    subgraph STORAGE["저장"]
        CloudWatch["CloudWatch Logs<br/>빠른 검색·탐지"]
        S3["Security Log S3<br/>원본·장기 보존"]
    end

    CFDelivery --> S3
    AWSDelivery --> CloudWatch
    AWSDelivery --> S3
    FluentBit --> CloudWatch
    EventBridge --> CloudWatch

    subgraph ANALYSIS["분석·탐지·알림"]
        LogsInsights["CloudWatch Logs Insights"]
        MetricFilter["Log Metric Filter"]
        Metric["Custom Metric"]
        Alarm["CloudWatch Alarm"]
        Athena["Amazon Athena"]
        SNS["Amazon SNS"]
    end

    CloudWatch --> LogsInsights
    CloudWatch -->|"auth.login.failed"| MetricFilter
    MetricFilter --> Metric
    Metric --> Alarm
    Alarm --> SNS
    S3 --> Athena
    EventBridge --> SNS

    subgraph EVIDENCE["조사·증거화"]
        Review["Scenario Query·Security Review"]
        Bundle["Local Evidence Bundle"]
    end

    LogsInsights --> Review
    Athena --> Review
    Review --> Bundle
```

> [!important] 읽는 기준
> 외부 HTTP 요청을 추적할 때 첫 관측 지점은 `CloudFront + WAF` Edge Boundary다.  
> CloudTrail, EKS Control Plane Log, VPC Flow Log, GuardDuty는 외부 요청 경로와 평행하게 시작되는 별도 관측 흐름이다.

> [!warning] 구성됨과 검증됨
> 이 지도는 현재 저장소에서 확인된 설계와 이전 Runtime Evidence를 함께 반영한다.  
> 특정 경로가 그려져 있다는 사실만으로 현재 Runtime에서 모든 전달·조회·탐지가 정상이라고 단정하지 않는다.

## 계층별 역할

| 계층 | 역할 |
|---|---|
| 생성 | 각 서비스가 자기 관점의 Log Event 또는 Finding 생성 |
| 전달 | Fluent Bit, AWS Log Delivery, EventBridge가 목적지로 전송 |
| 저장 | CloudWatch Logs는 빠른 검색, S3는 원본·장기 보존 |
| 분석 | Logs Insights와 Athena로 시간창·식별자 기반 조사 |
| 탐지 | Metric Filter·Custom Metric·Alarm 또는 GuardDuty Finding |
| 알림 | SNS가 Alarm과 GuardDuty Event를 구독자에게 전달 |
| 증거화 | Query, 결과, 실행 상태, Metadata를 Evidence Bundle에 보존 |

## 관련 노트

- [[08_Elastic Load Balancing ALB]]
- [[09_Amazon EKS]]
- [[18_Amazon S3]]
- [[20_Amazon CloudFront]]
- [[21_AWS WAF]]
- [[24_Amazon CloudWatch]]
- [[25_AWS CloudTrail]]
- [[26_Amazon GuardDuty]]
- [[27_Amazon EventBridge]]
- [[28_Amazon SNS]]
- [[30_VPC Flow Logs]]
- [[32_Fluent Bit]]
