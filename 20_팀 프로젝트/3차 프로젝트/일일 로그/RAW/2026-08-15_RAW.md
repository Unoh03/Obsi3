---
type: raw
status: active
created: "2026-08-15"
topic: "3차 프로젝트"
parent_moc: "[[00_3차프로젝트_목차]]"
---

# 2026-08-15 RAW

### 00:50

- Wazuh 원본화: Custom Rule `100100`을 Host Bind Mount로 전환하고 Host·Container SHA-256 일치와 `wazuh-analysisd -t=0`을 확인. `wazuh_manager.conf`에는 기존 CloudTrail S3 외에 WAF `aws-waf-logs-aws-topology-edge`(`us-east-1`)와 DVWA `/aws/eks/aws-topology-primary/dvwa`(`ap-northeast-2`) CloudWatch 입력을 추가함.
- Runtime 검증: Manager만 재생성한 뒤 `wazuh-modulesd -t=0`. AWS Module이 CloudWatch Service Analysis 두 건을 권한 오류 없이 완료했고 Archive에서 실제 WAF 요청 `4건`(`formatVersion=1`, `CF`, `POST`, `ALLOW`)과 DVWA Pod 로그 `13,817건`을 확인함.
- 판정: `CloudTrail + WAF + DVWA → Local Wazuh Archive` 연결 성공. DVWA 13,817건은 공격 횟수가 아니라 `2026-AUG-12` 이후 일반 출력·PHP 경고를 포함한 과거 로그 Backfill이므로 필터·Rule·Dashboard가 필요함.
- DVWA 관측 공백 보강: `security.command_injection.execution` 감사 Event와 IMDS 대상 분류(`ec2_imds|other`)를 `D:\DVWA`에 구현하고 정적 Test를 통과함. Raw Command·응답·Credential은 기록하지 않음. 변경 파일 3개는 아직 Commit·Push·Image Build·Argo 배포·Runtime 검증 전임.
- 추가 Source 조사: Primary ALB는 Security Log S3의 `alb/primary/AWSLogs/433048100798/elasticloadbalancing/ap-northeast-2/`에 표준 Access Log로 저장되며 Wazuh 공식 `bucket type="alb"` 지원. CloudFront는 `AWSLogs/433048100798/CloudFront/`의 JSON이지만 Wazuh 4.14.7 `AWSCustomBucket`은 `detail` Envelope 없는 현재 JSON을 버리므로 S3 `custom` 직결은 사용하지 않기로 함.
- 사용자 보고/남음: IAM Policy `aws-topology-wazuh-local-reader-policy`에 ALB Prefix 전용 `s3:GetObject` 문장 `ReadPrimaryAlbAccessLogObjects`를 추가·저장함(저장 후 AWS 재조회는 아직). 다음은 Policy 확인 → Wazuh ALB Bucket 설정·Archive 유입 검증 → CloudFront의 기존 S3 보관을 유지한 CloudWatch Logs 병렬 전달안 비용·Toggle 결정 → 새 DVWA Image 배포와 대표 공격 재검증 순서.
