---
type: project-daily-log
status: draft
created: 2026-08-02
log_date: 2026-08-02
project: 3차 프로젝트
project_moc: "[[20_팀 프로젝트/3차 프로젝트/00_3차프로젝트_목차]]"
source_raw: "[[20_팀 프로젝트/3차 프로젝트/RAW_메모]]"
---

# 2026-08-02 프로젝트 일일 로그

## 오늘 한 줄

- 두 차례 이상의 Daily Up/Down과 관측 Pipeline Smoke Test를 수행하고, IAM 권한 제거·로그인 실패 탐지·Athena 조회·DR 로그·Evidence Hash를 실제 Runtime으로 검증했지만 WAF Rate 차단은 제한된 실행 범위에서 입증하지 못했다.

## 수행·결과·검증

| 작업 | 결과 | 검증 / evidence |
|---|---|---|
| Query Runtime 보정 | 0건 결과 `$null`, CWLI 정규식, Windows UTF-8·UTC Parsing 결함 수정 | DVWA 181건 수집, EKS Query 44행·0행 성공, PowerShell 5.1·7 Test 통과 |
| 첫 Cold Up/Down | Daily 256개 생성 후 제거 | Up 32.9분, Down 27.4분; Argo `Synced / Healthy`, DVWA Ready, 종료 후 State·Runtime 0 |
| 정상 요청 상관관계 | CloudFront→ALB→DVWA 요청과 ALB Trace→BANK Request ID 연결 | 시각·Method·Path 연결, Application·ALB 차이 1ms 미만 |
| Evidence Pipeline | S3·CloudWatch·Query 결과와 Runtime Context 수집 | Smoke Bundle 69파일, Pre/Post-Down 105·104파일 Hash 불일치 0 |
| Daily Session 안전장치 | 5시간 Soft·6시간 Hard Deadline과 Watchdog 구현 | Lock 보존, 제한 재시도, Windows Scheduled Task Test 통과 |
| 두 번째 Baseline Cycle | Daily 256개 생성·제거, Argo·DVWA·외부 HTTP 확인 | Up 33.1분, Down 25.2분; Foundation 24개와 30일 로그 유지 |
| IAM-01 | 조치 전 Pod Identity로 S3 Put/Get/Delete 후 관련 6개 Resource 제거 | 조치 전 API 성공·정리, 조치 후 Credential 없음·S3 API 0건; Source 기본값 `false` |
| WEB-01 | before/count/block 각 로그인 실패 20회 | Application Event 각 20건, WAF COUNT 마지막 2건, BLOCK·HTTP 403은 0건 |
| 탐지·분석 | Login 실패 Metric Filter·Alarm·SNS Topic과 Athena Query Pack 구현 | Alarm `OK → ALARM → OK`, Athena 4개 Query `SUCCEEDED`; SNS Subscription 0 |
| DR Log | Tokyo 임시 Probe의 구조화 Event 전달 | DR Log Group에 Request ID 포함 Event 1건, Probe 즉시 삭제 |
| 최종 Evidence | WEB 3·Athena 4·DR 1 Bundle 생성 | 8개 Bundle, SHA-256 490개 불일치 0 |

## 결정

| 결정 | 이유 | 영향 |
|---|---|---|
| BANK Web Pod Identity 기본값을 `false`로 유지 | 권한 제거 후 다음 Up에서 재생성되지 않아야 함 | IAM-01 조치 상태가 Source에 고정됨 |
| S3 Data Event를 프로젝트 Prefix에 활성화 | IAM-01 조치 전후 API 증거가 필요 | Foundation Variable에 승인값 고정, Daily Down 뒤에도 로그 보존 |
| WEB-01 임시 Rule은 실행 후 `disabled` 복구 | 차단 미검증 상태에서 운영 동작을 바꾸지 않기 위함 | 추가 공격 없이 다음 Runtime 재검증 대기 |
| SNS Email Subscription은 비활성 | 수신자 확인과 Endpoint 보호가 필요 | Alarm 상태 전이는 검증했지만 실제 외부 알림은 미검증 |

## 막힌 점

| 문제 | 확인한 사실 | 다음 확인 |
|---|---|---|
| WAF Rate Rule 차단 미관측 | 20회·약 60초 실행에서 COUNT 2, BLOCK 0 | 전파·근사 완화 시간을 고려한 더 긴 bounded 실행으로 재검증 |
| 정상 Controller 활동이 Secret Query에 혼입 | 44행 전부 Argo CD·System Actor의 `watch/list` | Actor·Verb Baseline과 비정상 `exec`·거부를 분리 |
| Health Check Audit Noise | `/` 접근이 `authorization.access.denied`를 반복 생성 | `/login.php` Health Check 적용 후 Noise 감소 Runtime 확인 |
| SNS 실제 알림 미검증 | 확정 Subscription 0건 | 수신자를 정한 뒤 별도 검증 |

## 보안·비용·운영 주의

- `COUNT 2 / BLOCK 0`은 WAF 실패나 차단 성공이 아니라 제한된 실험에서 차단 효과가 입증되지 않았다는 뜻이다.
- EKS Control Plane Log가 Bundle에서 수만 건이므로 시나리오별 Actor·Verb·Time Window 제한이 필요하다.
- Daily Runtime은 두 Cycle 모두 제거했지만, 22:54에 다시 Up한 마지막 Session은 8월 3일 00:14 Down으로 이어졌다.

## 다음 작업

- [ ] 마지막 Daily Session을 안전하게 Down하고 Foundation 27개 보존 확인
- [ ] WAF Rate Rule 전파·완화 시간을 반영해 bounded 재검증
- [ ] 팀 본편 시나리오와 관측 검증 후보를 분리해 확정
- [ ] SNS 알림과 팀 Account 이식 경계를 후속 검토

## 보고서 재료

- 성과: 반복 가능한 Daily Lifecycle, IAM 권한 제거 전후, Login Alarm, Athena 4개 Query, DR Event와 490개 Hash 검증을 실제 Runtime으로 확보했다.
- 시행착오: 0건 Query, Windows Encoding·UTC, Bastion Host Key, 빈 CloudFront Object, WAF Rate 완화 지연을 실패와 구분해 보정했다.
- 그림 / 표 후보: `행위 → Application/WAF/AWS Log → Query/Alarm → 조치 → Evidence Bundle` 검증 Matrix.
