---
type: project-daily-log
status: draft
created: 2026-07-31
log_date: 2026-07-31
project: 3차 프로젝트
project_moc: "[[20_팀 프로젝트/3차 프로젝트/00_3차프로젝트_목차]]"
source_raw: "[[20_팀 프로젝트/3차 프로젝트/RAW_메모]]"
---

# 2026-07-31 프로젝트 일일 로그

## 오늘 한 줄

- 배포만 되던 BANK DVWA 환경에 Edge·Network·EKS·Application 로그와 Evidence Pipeline을 구현하고 실제 AWS 전달을 검증한 뒤, Daily Runtime을 제거해 로그와 증거가 Foundation에 남는 구조까지 확인했다.

## 수행·결과·검증

| 작업 | 결과 | 검증 / evidence |
|---|---|---|
| 관측성 기준점 고정 | CloudTrail·EKS Control Plane 외에 WAF·CloudFront·ALB·VPC Flow·DVWA 중앙 로그와 BANK Audit Log가 없음을 확인 | Argo CD `Synced / Healthy`, Pod Ready, 배포 Image SHA 일치 |
| Foundation·Daily 관측 구조 구현 | 로그 목적지와 30일 보존은 Foundation, 로그 발생 Resource와 Agent는 Daily Runtime이 담당하도록 분리 | Foundation/Daily State 소유권, 재계획 `No changes`, Daily Plan에서 기존 EKS Log Group 삭제 없음 |
| Edge·Network 로그 | CloudFront, ALB, VPC `REJECT`, WAF `COUNT`, EKS Control Plane 전달 구성 | CloudFront Object 2개, ALB Access Log 3개, WAF XSS Match Event 1건 확인 |
| DVWA Container Log | Primary·DR에 Fluent Bit와 Pod Identity 구성 | Primary DaemonSet `2/2`, DR `1/1` Ready; Primary 51건, DR Smoke Marker 1건 도착 |
| BANK 구조화 Audit Log | 로그인·회원가입·로그아웃·Security Level·접근 거부·CSRF 실패 Event를 JSON으로 기록 | 로그인 실패 Event 도착, 가짜 Password 노출 0건, PHP lint·Audit self-test·Docker build 통과 |
| Evidence Collector | UTC Time Window, Bounded Retry, Redaction, Strict Mode, Manifest와 SHA-256 추가 | Pre/Post-Destroy Bundle 131개·129개 파일 Hash 불일치 0 |
| CI/CD·GitOps | Audit Log 변경을 GitHub Actions→ECR→GitOps→Argo CD로 배포 | Runtime Image `sha-289c14e...`; 최신 Git 선언 `3be7f3b`는 다음 Up 재조정 대상으로 남음 |
| Daily Down | Daily Runtime 제거, Foundation 보존 | Daily State 0, 태그 기반 Runtime 0, Foundation State 24와 ECR·Security Log 보존 확인 |

## 결정

| 결정 | 이유 | 영향 |
|---|---|---|
| 로그 보존기간을 30일로 설정 | 7일보다 프로젝트 분석·보고서 증거 재사용에 적합 | CloudWatch와 S3 Current/Noncurrent Lifecycle을 30일로 통일 |
| DR에도 DVWA 로그 수집 준비 | Cross-Region DR 실험 증거가 필요 | Tokyo Fluent Bit와 DR Log Group을 Foundation/Daily 구조에 포함 |
| WAF는 우선 `COUNT` | 차단 전에 탐지 결과와 False Positive를 관찰 | XSS Match는 기록됐지만 최종 Action은 `ALLOW` |
| 로그 원본과 Application Runtime의 수명주기 분리 | Daily Destroy 뒤에도 증거를 남기기 위함 | Destination은 Foundation, Agent와 발생 Resource는 Daily가 소유 |

## 막힌 점

| 문제 | 확인한 사실 | 다음 확인 |
|---|---|---|
| CloudFront Delivery의 JSON Destination과 구분자 충돌 | `field_delimiter` 제거 후 Delivery 성공 | 다음 Up에서도 Object 자동 도착 확인 |
| Fluent Bit Filter 중첩으로 `CrashLoopBackOff` | 최상위 `additionalFilters`로 보정 후 Primary·DR Ready | Cold Start에서 자동 복원 확인 |
| 삭제 직후 Tagging API 오탐 | VPC Flow Log 실제 API에서는 0건 | 잔존 판정은 개별 AWS API를 우선 사용 |
| 최신 GitOps 선언과 종료 직전 Runtime 차이 | Source·ECR은 보존됐으나 Runtime은 이전 Revision | 다음 Daily Up에서 최신 Image 재조정 확인 |

## 보안·비용·운영 주의

- 공개 정상 요청에서 Apache·PHP 상세 Version Header와 Session·Security Cookie 발급을 확인했지만 실제 Cookie 값은 기록하지 않았다.
- Evidence 원본에는 접근권한이 필요한 CloudTrail Metadata가 포함될 수 있으므로 Git·Vault에 원본을 복사하지 않는다.
- Redaction Probe에서 가짜 Authorization·Cookie 값은 모두 `REDACTED`로 저장됐다.

## 다음 작업

- [ ] 빈 Daily State에서 Cold Up을 실행해 Agent·Application·Argo CD 자동 복원 확인
- [ ] 실제 Logs Insights Query와 0건 결과 처리 검증
- [ ] 대표 관측 검증 후보와 필요한 로그를 연결
- [ ] 다음 Up에서 최신 GitOps Revision과 Image 적용 확인

## 보고서 재료

- 성과: CloudFront→ALB→EKS→BANK DVWA 전 구간의 로그 배관과 Daily Destroy 후 증거 보존 구조를 실제 AWS에서 확인했다.
- 시행착오: CloudFront JSON 구분자 충돌, Fluent Bit Chart 설정 위치 오류, 삭제 직후 Tagging API 오탐을 증거 기반으로 보정했다.
- 그림 / 표 후보: `Log Source → Daily Agent/Resource → Foundation CloudWatch·S3 → Local Evidence Bundle` 흐름도.
